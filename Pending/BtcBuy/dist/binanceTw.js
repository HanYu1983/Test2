// Generated by LiveScript 1.6.0
(function(){
  var x$, ws, delay, click, scroll;
  console.log("tw");
  x$ = ws = new WebSocket('wss://stream.binance.com:9443/ws/xrpbtc@trade');
  x$.onopen = function(evt){
    return console.log(evt);
  };
  x$.onclose = function(evt){
    return console.log(evt);
  };
  x$.onmessage = function(arg$){
    var data, p;
    data = arg$.data;
    p = JSON.parse(data).p;
    return console.log(p);
  };
  x$.onerror = function(evt){
    return console.log(evt);
  };
  delay = function(t){
    return new Promise(function(res, rej){
      return setTimeout(res, t);
    });
  };
  click = function(elem){
    return $(elem).click();
  };
  scroll = function(elem, pos){
    return $(elem).scrollTop(pos);
  };
  async function main(){
    var scrollTarget, rows, infos, res$, i$, ref$, len$, row, grids, _, marketDom, priceDom, volume;
    (await delay(1000));
    scrollTarget = $(document).first();
    scrollTarget.scroll(function(){
      return console.log(scrollTarget.scrollTop());
    });
    click($('#tabContainer').find('div:contains("BTC")')[2]);
    (await delay(100));
    scroll(document, 4619);
    (await delay(500));
    rows = $('div[role="row"]');
    res$ = [];
    for (i$ = 0, len$ = (ref$ = rows.slice(1, rows.length)).length; i$ < len$; ++i$) {
      row = ref$[i$];
      grids = $(row).find('div[role="gridcell"]');
      console.log(grids[0]);
      _ = grids[0], marketDom = grids[1], _ = grids[2], priceDom = grids[3], _ = grids[4], _ = grids[5], volume = grids[6];
      res$.push($(marketDom).text());
    }
    infos = res$;
    return console.log(infos);
  }
  main();
}).call(this);
